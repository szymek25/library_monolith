spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER}
    hikari:
      connectionTimeout: 30000
      idleTimeout: 600000
      maxLifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_EMAIL_USERNAME}
    password: ${SPRING_EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: library-system
            client-authentication-method: none
            authorization-grant-type: authorization_code
            redirect-uri-template: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: openid,library-system

        provider:
          keycloak:
            authorization-uri: http://localhost:8080/realms/library/protocol/openid-connect/auth
            token-uri: http://keycloak:8080/realms/library/protocol/openid-connect/token
            user-info-uri: http://keycloak:8080/realms/library/protocol/openid-connect/userinfo
            userNameAttribute: preferred_username
            scope: openid,library-system
            jwk-set-uri: http://keycloak:8080/realms/library
      resourceserver:
        jwt:
#          issuer-uri: http://localhost:8080/realms/library
          jwk-set-uri: http://localhost:8080/realms/library/protocol/openid-connect/certs
